<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="mappers.basketMapper">

<select id="basketList" parameterType="string" resultType="model.BasketDTO">

	SELECT B.MEM_ID memId, B.MENU_ID menuId, M.MENU_IMG menuImg, M.MENU_NAME menuName, M.MENU_PRICE menuPrice, 
	B.MENU_COUNT menuCount, (M.MENU_PRICE*B.MENU_COUNT) totalPrice, U.ORDER_ADDRESS orderAddress
	FROM BASKET B
	INNER JOIN MENU M
	ON B.MENU_ID = M.MENU_ID AND B.MEM_ID = #{memId}
	
</select>

<delete id="basketDel" parameterType="string">

	DELETE FROM BASKET WHERE MENU_ID = #{menuId}

</delete>


</mapper>